 
 
 	// Average product  of 2 number..............
 	
 const prompt = require('prompt-sync')();
 
 let a = prompt("Enter the value a:");
 let b = prompt("Enter the  value b:");
 
 let p = a * b;
 console.log(p);
 
